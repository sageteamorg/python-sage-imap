.. _getting_started_index:

Getting started
===============

This section covers installation, your first connection, and the recommended APIs.

Suggested order
---------------

1. :doc:`installation` — install the package (and the ``[async]`` extra if needed)
2. :doc:`../tutorials/index` — **step-by-step Sync and Async tutorials** (recommended for learners)
3. :doc:`first_steps` — shorter quick-start reference
4. :doc:`imap_session` — ``IMAPSession`` facade reference
5. :doc:`async_api` — asyncio client (``sage_imap.aio``)
6. :doc:`migration_v2` — notes for upgrading to v2

.. toctree::
   :hidden:

   installation
   first_steps
   imap_session
   async_api
   migration_v2
   introduction
   what_is_imap
   terminologies
   features
   search
   message_set
   headers
   best_practices
   common_patterns
